# Basic Terraform project for a azure VM

My first Terraform Project to run a azurerm linux virtual machine using terraform.
Her you can clone the repository initially removing terraform.tstate, terraform.tfstate.backup and then run the following commands:
alias tf=terraform
'''bash
tf init
'''bash
tf plan 
'''bash
tf apply
